{{- if and .Values.raster.enabled .Values.raster.metrics.enabled .Values.raster.metrics.serviceMonitor.enabled (.Capabilities.APIVersions.Has "monitoring.coreos.com/v1/ServiceMonitor") }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ .Release.Name }}-raster
  labels:
    {{- include "eoapi.labels" . | nindent 4 }}
    app.kubernetes.io/component: raster
    {{- with .Values.raster.metrics.serviceMonitor.additionalLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  selector:
    matchLabels:
      app: {{ .Release.Name }}-raster
  endpoints:
    - port: http
      path: /metrics
      interval: {{ .Values.raster.metrics.serviceMonitor.interval }}
{{- end }}
