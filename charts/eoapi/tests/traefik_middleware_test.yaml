suite: traefik strip-prefix middleware
templates:
  - templates/_helpers/networking.tpl
  - templates/networking/ingress.yaml
  - templates/networking/traefik-middleware.yaml
tests:
  - it: "traefik strip-prefix middleware renders enabled service prefixes"
    template: templates/networking/traefik-middleware.yaml
    set:
      ingress.className: "traefik"
      stac.enabled: true
      raster.enabled: true
      vector.enabled: false
      multidim.enabled: true
      browser.enabled: false
    asserts:
      - isKind:
          of: Middleware
      - equal:
          path: metadata.name
          value: RELEASE-NAME-strip-prefix-middleware
      - equal:
          path: spec.stripPrefix.prefixes
          value:
            - /stac
            - /raster
            - /multidim

  - it: "excludes auth-proxied stac from strip-prefix prefixes"
    template: templates/networking/traefik-middleware.yaml
    set:
      ingress.className: "traefik"
      stac.enabled: true
      stac-auth-proxy.enabled: true
      raster.enabled: true
      vector.enabled: false
      multidim.enabled: false
      browser.enabled: false
    asserts:
      - isKind:
          of: Middleware
      - equal:
          path: spec.stripPrefix.prefixes
          value:
            - /raster

  - it: "omits the strip-prefix annotation when no middleware renders, so the router reference never dangles (#568)"
    set:
      ingress.className: traefik
      ingress.host: eoapi.local
      browser.enabled: true
      browser.ingress.path: "/"
      stac.enabled: false
      raster.enabled: false
      vector.enabled: false
      multidim.enabled: false
      docServer.enabled: false
    asserts:
      - template: templates/networking/traefik-middleware.yaml
        hasDocuments:
          count: 0
      - template: templates/networking/ingress.yaml
        notExists:
          path: metadata.annotations["traefik.ingress.kubernetes.io/router.middlewares"]
